package com.example.store7app

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.ArrayAdapter
import android.widget.EditText
import android.widget.Spinner
import android.widget.TextView

class Cereales : AppCompatActivity() {

    private var arroz: TextView? = null
   // private val spnArroz:Spinner?=null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_cereales)

    }}
       // var spnArroz: (Spinner) = findViewById<Spinner>(R.id.listArroz)

       // val spnArroz: Spinner = findViewById(R.id.listArroz)

        //val lista = listOf("0","1","2","3","4","5")

        //val adaptador =  ArrayAdapter(this,android.R.layout.simple_spinner_item,lista)
       // spnArroz.adapter

     //   val spinner: Spinner = findViewById(R.id.listArroz)
// Create an ArrayAdapter using the string array and a default spinner layout
  //      ArrayAdapter.createFromResource(
   //         this,
         //   R.array.planets_array,
      //      android.R.layout.simple_spinner_item
       // ).also { adapter ->
            // Specify the layout to use when the list of choices appears
         //   adapter.setDropDownViewResource(android.R.layout.simple_spinner_dropdown_item)
            // Apply the adapter to the spinner
           // spinner.adapter = adapter
      //  }










        //arroz!!.text = "    Arroz = xLB "+ arrozLb




     //   var arrozLb = 1200
      //  arroz=findViewById(R.id.tvArroz)
    //}
//}